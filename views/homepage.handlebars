<div class="container">
    <div class="row">
    {{#each blogs as |blog|}}
    <div class="col-sm-12 col-md-6" style="margin-bottom: 10">
    {{> blog-info}}
    </div>
    {{/each}}
    </div>
</div>